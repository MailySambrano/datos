#!/bin/bash

# Directorio de origen
source_dir="$HOME/Escritorio/tu_carpeta"

# Directorio de destino en red
dest_dir="/ruta/a/tu/carpeta/en/red"

# Copiar archivos nuevos creados en las últimas 24 horas
find "$source_dir" -type f -mtime -1 -exec cp {} "$dest_dir" \;



chmod +x /ruta/a/copiar_archivos.sh
0 0 * * * /ruta/a/copiar_archivos.sh
